from setuptools import setup, find_packages
setup(
    name = 'pyy',
    version = '0.9',
    packages=find_packages(),

    author = 'Tom Flanagan and Jake Wharton',
    license = 'LGPL',
    description = 'pyy is a python library for creating (X)HTML pages with the use of objects. This allows you to tightly integrate (X)HTML generation into your backend without the need of using an intermediate templating language.',
    url = 'http://github.com/Knio/pyy/tree/master',
    
    
)

